info:
  name: Search
  type: http
  seq: 1

http:
  method: GET
  url: "{{core_url}}/api/search?query=TSLA"
  params:
    - name: query
      value: TSLA
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
