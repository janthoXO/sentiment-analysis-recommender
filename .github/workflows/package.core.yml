name: Package Core

on:
  pull_request:
    branches:
      - main
    paths:
      - "core/**"
      - ".github/workflows/build.core.yml"
      - ".github/workflows/package.core.yml"
  workflow_call:
    inputs:
      version:
        required: false
        type: string
        default: 'latest'

jobs:
  build:
    uses: ./.github/workflows/build.core.yml

  package:
    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        working-directory: core

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          package_json_file: core/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: core/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Set package.json version
        if: ${{ inputs.version && inputs.version != 'latest' }}
        run: pnpm version ${{ inputs.version }} --no-git-tag-version --allow-same-version

      - name: Build Docker Image
        run: docker build -t core:${{ inputs.version || 'latest' }} .

      - name: Save Docker Image
        run: docker save core:${{ inputs.version || 'latest' }} -o core-${{ inputs.version || 'latest' }}.tar

      - name: Upload Docker Image
        uses: actions/upload-artifact@v6
        with:
          name: docker-image-${{ inputs.version || 'latest' }}
          path: core/core-${{ inputs.version || 'latest' }}.tar
          if-no-files-found: error
