---
applyTo: "webclient/**/*,.github/workflows/**/*.webclient.yml"
---

use react typescript with shadcn and tailwind
use zod for dtos
use pnpm as the package manager and always `pnpm run lint` and `pnpm run build` to verify working code
always work in the /webclient directory